import 'package:flutter/material.dart';

class MbeleColors {
  // Primary (Deep Orange / Mango) - Strong and warm.
  static const Color primaryOrange = Color(0xFFFF8C00);
  // Accent (Vibrant Yellow / Gold) - Energetic and visible.
  static const Color accentYellow = Color(0xFFFFD700);
  // Highlight (Fuchsia / Deep Pink) - Action-oriented and modern.
  static const Color highlightPink = Color(0xFFE91E63);
  // Text & Background
  static const Color backgroundLight = Color(0xFFFFF8E1);
  static const Color textDark = Color(0xFF333333);
}